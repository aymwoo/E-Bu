services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile.cn
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
      - DB_PATH=/data/ebu.db
    volumes:
      - ./data:/data
    restart: unless-stopped
    networks:
      - ebu-network

  frontend:
    build:
      context: .
      dockerfile: Dockerfile.frontend.build.cn
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - backend
    networks:
      - ebu-network
    restart: unless-stopped

networks:
  ebu-network:
    driver: bridge
